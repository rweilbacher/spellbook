<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Voice notes. RECORD_AUDIO is asked for the first time you tap the mic,
         never at launch. MODIFY_AUDIO_SETTINGS is what lets us route input to a
         Bluetooth headset; it is granted at install, not at runtime. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- A reboot clears the widget's midnight alarm and the reminder alarms;
         this is how they get re-set without waiting for you to open the app. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Daily reminders. Asked for the first time you set one, never at launch;
         with no times set the app never posts anything and never asks. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The home screen widget. Exported because the broadcasts that keep it
             honest — the system's widget update, boot, a clock or timezone
             change — all arrive from outside the app. Nothing they can trigger
             does more than re-read the book and redraw. -->
        <receiver
            android:name=".SpellWidget"
            android:exported="true"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/spell_widget_info" />
        </receiver>

        <!-- The daily reminders. The alarm itself arrives as an explicit intent
             from our own PendingIntent and needs no filter; the four broadcasts
             below are the ones that invalidate an armed alarm — a reboot, an
             update, and either half of the clock moving. Exported because they
             come from outside the app. Nothing they can trigger does more than
             re-read the times and re-arm. -->
        <receiver
            android:name=".ReminderReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
